{
  "name": "@inkress/embed-sample",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Reference implementation of an Inkress embedded app — exercises every primitive in @inkress/app-bridge.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "start": "node server.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@inkress/app-bridge": "github:jamlance/app-bridge",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
